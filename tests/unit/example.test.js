/**
 * Example Unit Test
 * مثال على اختبار وحدة
 */

describe('Example Test Suite', () => {
  test('should pass basic test', () => {
    expect(true).toBe(true);
  });

  test('should perform simple math', () => {
    expect(2 + 2).toBe(4);
  });
});
