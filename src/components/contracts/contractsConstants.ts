/** Responsive UI page size (web list pagination). */
export const CONTRACTS_PAGE_SIZE = 16;

/** Desktop SQL-backed contract picker page size (stable; do not tie to UI breakpoints). */
export const CONTRACTS_FAST_PAGE_SIZE = 16;
