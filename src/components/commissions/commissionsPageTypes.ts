/**
 * إعادة تصدير فقط — التعريف الفعلي في `@/hooks/useCommissions` (`CommissionsPageModel`)
 * حتى لا يتباعد أي ملف مع `ReturnType<typeof useCommissions>`.
 */
export type { CommissionsPageModel } from '@/hooks/useCommissions';
