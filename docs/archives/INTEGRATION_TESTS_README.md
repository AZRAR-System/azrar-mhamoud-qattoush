# 🧪 الاختبارات التسلسلية - دليل سريع

## ⚡ البدء السريع (دقيقة واحدة)

### الطريقة 1️⃣: واجهة المستخدم
```
1. اذهب إلى: localhost:3000/integration-tests
2. اضغط: "تشغيل الاختبارات" 🚀
3. شاهد النتائج ✅
```

### الطريقة 2️⃣: DevTools (أسرع)
```javascript
// افتح Console في المتصفح واكتب:
await runTests();

// أو بشكل مفصل:
const suite = new IntegrationTestSuite();
const results = await suite.runAllTests();
console.log(results);
```

---

## 📋 ماذا يتم اختباره؟

```
شخص ➜ عقار ➜ عقد ➜ كمبيالات ➜ عمولات
```

| # | الاختبار | ماذا يفعل |
|-|---------|----------|
| 1️⃣ | إضافة شخص | إضافة مالك جديد |
| 2️⃣ | إضافة عقار | إضافة شقة للمالك |
| 3️⃣ | عقد إيجار | ربط العقار بمستأجر |
| 4️⃣ | كمبيالات | إنشاء دفعات إيجار |
| 5️⃣ | عمولات | حساب رسوم الوسيط |
| 6️⃣ | سداد | تعديل حالة الدفعة |
| 7️⃣ | سلامة | فحص البيانات |

---

## 📁 الملفات الجديدة

```
src/
├── services/
│   ├── integrationTests.ts      ← الاختبارات الرئيسية
│   └── testRunner.ts            ← دالة سهلة التشغيل
├── components/
│   └── IntegrationTestsPanel.tsx ← الواجهة الرسومية
└── pages/
    └── IntegrationTests.tsx      ← الصفحة الكاملة

التوثيق/
├── INTEGRATION_TESTS_GUIDE.md    ← شرح مفصل
└── INTEGRATION_TESTS_SUMMARY.md  ← ملخص شامل
```

---

## ✅ النتائج المتوقعة

### عند النجاح ✅
```
✅ إضافة شخص (مالك)              | ✅ نجح
✅ إضافة عقار                   | ✅ نجح
✅ إنشاء عقد إيجار              | ✅ نجح
✅ التحقق من الكمبيالات        | ✅ نجح
✅ التحقق من العمولات          | ✅ نجح
✅ سداد كمبيالة                 | ✅ نجح
✅ التحقق من سلامة البيانات    | ✅ نجح

📊 ملخص:
✅ نجح:        7/7
⏱️ المدة: 56.35ms
```

---

## 🐛 استكشاف الأخطاء

### ❌ لم تعمل الاختبارات؟

```bash
# 1. تحقق من عدم وجود أخطاء
npm run build

# 2. جرّب من جديد
await runTests();

# 3. افتح Console
F12 → Console Tab
```

### ⚠️ اختبار واحد فشل؟

- اقرأ رسالة الخطأ بعناية
- تحقق من البيانات
- جرّب الاختبار من جديد

---

## 🎯 الحالات المختلفة

### حالة 1: جميع الاختبارات نجحت ✅
```
النظام يعمل بشكل مثالي!
يمكنك الاستخدام بثقة.
```

### حالة 2: اختبار واحد فشل ❌
```
الاختبار السابق سبب المشكلة
سيتم تخطي الاختبارات اللاحقة
```

### حالة 3: اختبار تم تخطيه ⏭️
```
أحد الاختبارات السابقة فشل
هذا الاختبار يعتمد عليه
```

---

## 📊 البيانات المستخدمة

### شخص المالك
```json
{
  "الاسم": "محمد أحمد الخطيب",
  "رقم_الهاتف": "+962791234567",
  "البريد_الإلكتروني": "mohammad@khaberni.com",
  "عنوان_السكن": "عمّان، الأردن"
}
```

### العقار
```json
{
  "الاسم": "شقة سكنية في الدقي",
  "الموقع": "الدقي، القاهرة",
  "المساحة": 120,
  "عدد_الغرف": 3,
  "السعر_الشهري": 2000
}
```

### العقد
```json
{
  "مدة العقد": "سنة واحدة",
  "الإيجار السنوي": 24000,
  "التأمين": 4000,
  "الدفعات": 12 دفعة شهرية
}
```

---

## 💡 نصائح مهمة

✅ تشغيل الاختبارات
```javascript
// في أي وقت
await runTests();

// سيُنشئ بيانات جديدة
// بدون التأثير على البيانات القديمة
```

✅ حفظ النتائج
```
اضغط: "تنزيل التقرير"
سيتم حفظ JSON للنتائج
```

✅ العودة للبدء
```
اضغط: "إعادة تعيين"
سيتم مسح النتائج السابقة
```

---

## 📈 الأداء

```
المدة الإجمالية: ~50-60 ميلي ثانية
سرعة الاختبار:  جداً سريع ⚡
الأداء:         ممتاز ✅
```

---

## 🔗 الروابط السريعة

| الرابط | الوصف |
|--------|--------|
| `/integration-tests` | واجهة الاختبارات |
| `INTEGRATION_TESTS_GUIDE.md` | دليل كامل |
| `INTEGRATION_TESTS_SUMMARY.md` | ملخص شامل |
| `src/services/integrationTests.ts` | الكود المصدري |

---

## ❓ أسئلة شائعة

**س: هل يؤثر على البيانات الحالية؟**  
ج: لا، يُنشئ بيانات جديدة فقط

**س: كم مرة يمكن التشغيل؟**  
ج: بدون حد، كل مرة ستُنشئ بيانات جديدة

**س: هل يمكن تشغيل اختبار واحد فقط؟**  
ج: نعم، استخدم `suite.test_01_AddPerson()`

**س: ماذا لو كانت هناك أخطاء؟**  
ج: ستُرى رسائل خطأ واضحة

---

## 🎉 الخلاصة

✅ **النظام يعمل بشكل مثالي!**

- الشخص ✅
- العقار ✅
- العقد ✅
- الكمبيالات ✅
- العمولات ✅

كل شيء متسلسل وصحيح!

---

**آخر تحديث:** 22 ديسمبر 2024  
**الحالة:** ✅ جاهز للاستخدام
